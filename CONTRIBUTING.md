# Contributing

## Setup

```bash
pnpm install
pnpm check
```

## Development workflow

1. Create a branch.
2. Implement changes with tests.
3. Run `pnpm check`.
4. Open a pull request.

## Standards

- Keep `.agents` as canonical source behavior.
- Preserve cross-provider sync compatibility.
- Add tests for parser or adapter changes.
